[project]
name = "discordrpg"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "asyncpg>=0.31.0",
    "discord-py>=2.7.1",
    "python-dotenv>=1.2.2",
]

[project.scripts]
discordrpg = "discordrpg:main"

[tool.uv]
package = false
